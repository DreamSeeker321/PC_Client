var db=new SQL.Database;db.run("CREATE TABLE test (col1, col2);"),db.run("INSERT INTO test VALUES (?,?), (?,?),(?,?)",[1,111,2,222,3,333]);var stmt=db.prepare("SELECT * FROM test WHERE col1 BETWEEN $start AND $end");for(stmt.getAsObject({$start:1,$end:1}),stmt.bind({$start:1,$end:2});stmt.step();){var row=stmt.getAsObject();console.log(row)}